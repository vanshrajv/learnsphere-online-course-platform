const navigation = [['overview', 'Overview', '⌂'], ['courses', 'Explore courses', '⌕'], ['library', 'My learning', '▣'], ['certificates', 'Certificates', '◇']]

export default function Sidebar({ activeView = 'overview', onNavigate, theme = 'light', onToggleTheme, onHelp }) {
	return <aside className="sidebar"><button className="brand" onClick={() => onNavigate?.('overview')} aria-label="Go to overview"><span className="brand-mark">L</span><span>learn<span>sphere</span></span></button><div className="profile-mini"><div className="avatar">VR</div><div><strong>Vansh Raj</strong><span>Curious learner</span></div></div><nav className="main-nav" aria-label="Main navigation"><span className="nav-label">Workspace</span>{navigation.map(([id, label, icon]) => <button key={id} className={`nav-item ${activeView === id ? 'active' : ''}`} onClick={() => onNavigate?.(id)}><span className="nav-icon">{icon}</span>{label}{id === 'library' && <span className="nav-count">3</span>}</button>)}</nav><div className="sidebar-bottom"><div className="streak-card"><div className="flame">✹</div><div><strong>7 day streak</strong><span>Keep your rhythm going</span></div></div><button className="nav-item" onClick={onToggleTheme}><span className="nav-icon">{theme === 'light' ? '☾' : '☀'}</span>{theme === 'light' ? 'Night mode' : 'Day mode'}</button><button className="nav-item muted" onClick={onHelp}><span className="nav-icon">?</span>Help center</button></div></aside>
}
